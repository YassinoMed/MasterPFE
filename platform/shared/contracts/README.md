# Shared Contracts

This folder will host:

- shared API schemas
- DTO contracts
- enum references
- cross-service payload examples

The goal is contract consistency without introducing a shared business monolith.
