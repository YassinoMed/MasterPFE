# ── SecureRAG Hub — Secrets Directory ──────────────────────────
# Tous les fichiers dans ce répertoire sont des credentials.
# Ils ne doivent JAMAIS être commités dans Git.
# Utiliser SOPS + age pour les secrets versionnés (infra/secrets/*.enc.yaml)
# ou HashiCorp Vault + External Secrets Operator en production.
*
*/
!.gitignore
