apiVersion: v1
kind: Service
metadata:
  name: {{ include "soc2-compliance-engine.fullname" . }}
  labels:
    {{- include "soc2-compliance-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "soc2-compliance-engine.selectorLabels" . | nindent 4 }}
