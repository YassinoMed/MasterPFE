apiVersion: v1
kind: ResourceQuota
metadata:
  name: securerag-prod-quota
  namespace: securerag-prod
spec:
  hard:
    requests.cpu: "4"
    requests.memory: "6Gi"
    limits.cpu: "12"
    limits.memory: "12Gi"
    pods: "40"
    services: "20"
    persistentvolumeclaims: "10"
