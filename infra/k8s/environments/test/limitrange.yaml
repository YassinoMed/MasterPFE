apiVersion: v1
kind: LimitRange
metadata:
  name: securerag-test-limits
  namespace: securerag-test
spec:
  limits:
  - default:
      cpu: 500m
      memory: 512Mi
    defaultRequest:
      cpu: 100m
      memory: 128Mi
    type: Container
