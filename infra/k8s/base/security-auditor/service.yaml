apiVersion: v1
kind: Service
metadata:
  name: security-auditor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: security-auditor
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
