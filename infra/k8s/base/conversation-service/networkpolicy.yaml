apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: conversation-service-network
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: conversation-service
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: portal-web
      ports:
        - protocol: TCP
          port: 8000
  egress: []
