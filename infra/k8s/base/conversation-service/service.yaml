apiVersion: v1
kind: Service
metadata:
  name: conversation-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: conversation-service
  ports:
    - name: http
      port: 8000
      targetPort: http
