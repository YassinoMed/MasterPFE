apiVersion: v1
kind: Service
metadata:
  name: postgres-auth
  namespace: securerag-hub
spec:
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    app.kubernetes.io/name: postgres-auth
