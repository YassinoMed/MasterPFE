apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Fondation de securerag-hub : namespace + quota + limitrange historiques.
# Sortis de infra/k8s/base le 2026-09-25 pour permettre les overlays par
# environnement (chaque env porte son propre socle via infra/k8s/environments/<env>/).
resources:
  - namespace.yaml
  - resourcequota.yaml
  - limitrange.yaml
