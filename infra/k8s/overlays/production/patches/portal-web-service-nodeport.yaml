apiVersion: v1
kind: Service
metadata:
  name: portal-web
  annotations:
    securerag.io/exposure: production-kind-nodeport
spec:
  type: NodePort
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
      nodePort: 30081
