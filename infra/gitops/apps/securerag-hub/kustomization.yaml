apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../k8s/overlays/production

# Jenkins is still the CI and supply-chain authority. This file is updated by
# scripts/gitops/update-digests.sh after image scan/sign/verify/promote steps
# have produced artifacts/release/promotion-digests-cluster.txt.
images: []
